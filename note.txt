git init
git add *
git commit -m "say something"

first pull from github
git remote add origin https://github.com/jademount/<project name>

create a repository in github.com

git remote add origin {git repository url}

if key in wrong url in previous step,correct it with:git remote set-url origin {git repository url}

git push -u origin master

found a folder staged and want to remove:
git rm -r --cached {foldername}

setting FLASK_APP environment variable:
export FLASK_APP=microblog.py
